{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:wasm": "cargo build --release --target wasm32-unknown-unknown --lib --manifest-path ../../services/ingress-engine/Cargo.toml && mkdir -p wasm && cp ../../services/ingress-engine/target/wasm32-unknown-unknown/release/ingress_engine.wasm wasm/",
    "start": "next start",
    "lint": "eslint",
    "convex:dev": "convex dev",
    "convex:codegen": "convex codegen"
  },
  "dependencies": {
    "@clerk/nextjs": "^6.24.0",
    "convex": "^1.40.0",
    "next": "16.2.7",
    "postmark": "^4.0.7",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.7",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
